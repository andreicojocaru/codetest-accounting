# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:
# this can be a template, kept it like this for clarity 
- task: DotNetCoreCLI@2
  displayName: "Publish service: Accounts"
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '$(Build.SourcesDirectory)/src/CodeTest.Accounting.Accounts --output $(Build.ArtifactStagingDirectory)'

- task: DotNetCoreCLI@2
  displayName: "Publish service: Consumers"
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '$(Build.SourcesDirectory)/src/CodeTest.Accounting.Consumers --output $(Build.ArtifactStagingDirectory)'

- task: DotNetCoreCLI@2
  displayName: "Publish service: Transactions"
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '$(Build.SourcesDirectory)/src/CodeTest.Accounting.Transactions --output $(Build.ArtifactStagingDirectory)'

- task: DotNetCoreCLI@2
  displayName: "Publish service: BFF"
  inputs:
    command: 'publish'
    publishWebProjects: true
    arguments: '$(Build.SourcesDirectory)/src/CodeTest.Accounting.BFF --output $(Build.ArtifactStagingDirectory)'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'services'
    publishLocation: 'Container'